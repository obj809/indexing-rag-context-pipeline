# Copy to .env and fill in. The indexer reads its own .env first, then falls back
# to the umbrella project's .env during the multi-repo split.
# Only DATABASE_URL is needed — embedding runs locally, no API key.
DATABASE_URL=postgresql://rag:rag@localhost:5432/rag
