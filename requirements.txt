# Pinned to a known-good set: this repo defines the embedding contract (model +
# vector dimension) the engine queries against, so the index must be reproducible.
# torch is NOT here — it's installed from the CPU-only index in the Dockerfile
# (PyPI's default wheels bundle multi-GB CUDA libs); its pin lives there.
pymupdf4llm==1.27.2.3
langchain-core==1.4.3
langchain-text-splitters==1.1.2
sentence-transformers==5.5.1
numpy==2.4.6
python-dotenv==1.2.2
psycopg[binary]==3.3.4
pgvector==0.4.2
